{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ocex.io/schemas/catalog.json",
  "title": "OcexCatalog",
  "description": "An OCEX product catalog document, equivalent to EDI 832. Represents a vendor's price/sales catalog transmitted to a buyer.",
  "type": "object",
  "required": [
    "id",
    "version",
    "type",
    "sender",
    "receiver",
    "documentDate",
    "catalogId",
    "effectiveDate",
    "items"
  ],
  "properties": {
    "id": {
      "type": "string",
      "description": "Unique identifier for this document."
    },
    "version": {
      "type": "string",
      "description": "OCEX protocol version string (e.g. \"1.0\")."
    },
    "type": {
      "type": "string",
      "const": "catalog",
      "description": "Discriminator field identifying this as a catalog document."
    },
    "sender": {
      "$ref": "#/$defs/OcexParty",
      "description": "The party sending this catalog (typically the vendor)."
    },
    "receiver": {
      "$ref": "#/$defs/OcexParty",
      "description": "The party receiving this catalog (typically the buyer)."
    },
    "documentDate": {
      "type": "string",
      "format": "date",
      "description": "ISO 8601 date on which the catalog was generated (YYYY-MM-DD)."
    },
    "catalogId": {
      "type": "string",
      "description": "Vendor-assigned catalog identifier, unique within the sender's system."
    },
    "effectiveDate": {
      "type": "string",
      "format": "date",
      "description": "ISO 8601 date from which the catalog prices are effective (YYYY-MM-DD)."
    },
    "expirationDate": {
      "type": "string",
      "format": "date",
      "description": "Optional ISO 8601 date after which the catalog expires (YYYY-MM-DD)."
    },
    "items": {
      "type": "array",
      "items": { "$ref": "#/$defs/OcexLineItem" },
      "minItems": 1,
      "description": "One or more catalog items with pricing information."
    },
    "metadata": {
      "type": "object",
      "additionalProperties": true,
      "description": "Arbitrary key-value metadata for extension or passthrough data."
    }
  },
  "additionalProperties": false,
  "$defs": {
    "OcexParty": { "$ref": "common.json#/$defs/OcexParty" },
    "OcexLineItem": { "$ref": "common.json#/$defs/OcexLineItem" }
  }
}
